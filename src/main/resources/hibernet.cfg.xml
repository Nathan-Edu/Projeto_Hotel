<hibernate-configuration>
    <session-factory>

        <!-- Configurações Básicas da Conexão -->
        <property name="hibernate.connection.driver_class">org.postgresql.Driver</property>
        <property name="hibernate.connection.url">jdbc:postgresql://localhost:5432/Dev</property>
        <property name="hibernate.connection.username">postgres</property>
        <property name="hibernate.connection.password">root</property>

        <!-- Outras Configurações -->
        <property name="hibernate.show_sql">true</property>
        <property name="hibernate.format_sql">true</property>

        <!-- Configuração de Criação do Banco de Dados -->
        <property name="hbm2ddl.auto">update</property>

        <!-- Mapeamento das Entidades -->
        <mapping
                class="br.edu.ifpr.bsi.projetodisciplina.model.Funcionario" />
    </session-factory>
</hibernate-configuration>